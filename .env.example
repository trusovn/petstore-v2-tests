# Copy this file to .env and fill in the value.
# .env is gitignored; never commit real keys.
#
# PETSTORE_API_KEY is required by the SUT contract tests (make verify).
# Set it to the api_key your Petstore instance accepts. The loader fails
# fast if it is unset or blank.
PETSTORE_API_KEY=
